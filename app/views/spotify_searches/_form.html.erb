<%= form_with(model: spotify_search) do |form| %>
  <% if spotify_search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spotify_search.errors.count, "error") %> prohibited this spotify_search from being saved:</h2>

      <ul>
        <% spotify_search.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uuid %>
    <%= form.text_field :uuid %>
  </div>

  <div class="field">
    <%= form.label :results %>
    <%= form.text_area :results %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
